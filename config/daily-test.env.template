# SAI Daily Test Service Configuration
# End-to-end alert system validation

# =============================================================================
# n8n Webhook Configuration
# =============================================================================
# The n8n webhook URL to send test images to
N8N_WEBHOOK_URL=https://n8n.altermundi.net/webhook/8ecbf1c4-3bcd-4ead-8dec-3dde52f5b40f

# Authorization Bearer token for n8n webhook
N8N_AUTH_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6Im44bldlYmhvb2tBY2Nlc3MiLCJpYXQiOjE2NzY0MDI4MDAsImV4cCI6MTcwNzk2MDgwMCwiaXNzIjoibjhuQXBpIiwiYXVkIjoid2ViaG9va0NsaWVudCJ9.HKt6HB1KChxEXUusXBFrFupyxUhr0C2WW5IEfKwYZnw

# =============================================================================
# Test Images and Labels Configuration  
# =============================================================================
# Directory containing test images (relative to project root)
TEST_IMAGES_DIR=tests/images

# Directory containing test labels (relative to project root)
# Label files provide additional metadata for test images
TEST_LABELS_DIR=tests/labels

# Subdirectories for different detection types
FIRE_SUBDIR=fire
SMOKE_SUBDIR=smoke
BOTH_SUBDIR=both

# =============================================================================
# Test Metadata Configuration
# =============================================================================
# Location identifier sent with test images
TEST_LOCATION=TestLab

# Device ID sent with test images  
TEST_DEVICE_ID=sai-test-runner-01

# Whether to randomize location and device_id for more realistic testing
RANDOMIZE_METADATA=true

# =============================================================================
# Test Categories Configuration
# =============================================================================
# Enable/disable specific test categories
ENABLE_FIRE_TESTS=true
ENABLE_SMOKE_TESTS=true
ENABLE_BOTH_TESTS=true

# =============================================================================
# HTTP Request Configuration
# =============================================================================
# Request timeout in seconds
REQUEST_TIMEOUT=30

# Maximum retry attempts for failed requests
MAX_RETRIES=3

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Log file location (ensure directory exists and is writable)
LOG_FILE=/var/log/sai-inference/daily-test.log

# =============================================================================
# Dynamic Cron Schedule Configuration
# =============================================================================
# Cron schedule in standard cron format (minute hour day month weekday)
CRON_SCHEDULE=0 9,17 * * *

# Human-readable description of the schedule
CRON_DESCRIPTION=Twice daily at 9:00 AM and 5:00 PM

# Whether to enable the cron job (true/false)
CRON_ENABLED=true

# Management: python src/daily_test.py --update-cron
# Examples: 0 9 * * 1-5 (weekdays), 0 */6 * * * (every 6h)
